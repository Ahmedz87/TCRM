# Standalone TradeSoft CRM copy — backend config
# Points at the dedicated copied database (NOT the live broker_crm).
TRADESOFT_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/tradesoft_crm
